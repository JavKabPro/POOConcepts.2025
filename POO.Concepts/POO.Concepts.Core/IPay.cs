namespace POO.Concepts.Core;

public interface IPay   //La interfaz no tiene definiciones de método, lo único que tiene es lo que debe implementar
{                       //las clases
    decimal GetValueToPay();
}
